## VARIABLES are used for storing data, giving meaning to data, and making 
# code readable and understandable. e.g

## Variables allow user interaction.
# name = input('Enter your name ')
# print(name.title())

## They help connect different parts of your program
name = 'baron dikachi'       #box name → contains "baron dikachi"
display = (f'Hello {name.title()} the tech bro.')  
# print(display)

## VARIABLES are used in operations (logic & calculations.
# a = 5
# b = 5
# sum = a + b
# print(sum)

character_name = "dikachi sunday" 
rule = 'python'
rule1 = 'No skipping' 
## A formatted string (or f-string) is a way to insert variables or expressions directly inside a string.

print(f"\n=={character_name.upper()}'S CODING JOURNEY==\n")
print(f"This is {character_name.title()} first python code.")
print(f"While learning {rule} i must not understand everything at once.")
print(f"One topic per-day {rule1} at all regardless the situation")
print(f"After learning, {character_name.title()} will always try coding one task per day.") #They let you reuse data
print(f"{character_name.title()} will never give up 💪")


# 🔥 Key Concepts You Used
# Variables → storing data
# Strings → text inside quotes
# f-strings → inserting variables inside text
# String method .title() → capitalizes each word
# print() → shows output